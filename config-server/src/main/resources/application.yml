spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nursultanaslan/survey-app.git
          search-paths:
            - configurations/
            - configurations/{application}
          default-label: main
server:
  port: 8787
